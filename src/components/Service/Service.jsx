import { Outlet } from "react-router";
import ServiceHome from "./ServiceHome";

const Service = () => {

    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default Service;